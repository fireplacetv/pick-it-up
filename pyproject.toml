[project]
name = "pick-it-up"
version = "0.1.0"
description = "Simple web app to notify residents when they have package deliveries"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["flask"]
